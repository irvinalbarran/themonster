# Frontend: Netlify
[build]
  command = "echo 'Static site - no build required'"
  publish = "."

# API proxy disabled - frontend uses Backend URL from admin panel (getBackendUrl())

# Serve index.html for all routes (SPA fallback)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
